// Bob's financial details
let salary = 1000;
let rent = 375;
let bills = 250;

// Calculate total expenses
let totalExpenses = rent + bills;

// Calculate money left
let moneyLeft = salary - totalExpenses;

// Print results
console.log(`Bob's salary: ${salary} EUR`);
console.log(`Total expenses: ${totalExpenses} EUR`);
console.log(`Money left for the month: ${moneyLeft} EUR`);

alert(`Bob's salary: ${salary} EUR\nTotal expenses: ${totalExpenses} EUR\nMoney left: ${moneyLeft} EUR`);
